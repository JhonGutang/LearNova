type CoursesPage {
    student: StudentProfile!
    courses: [Course!]!
}

enum CourseCategory {
    ALL
    FEATURED
    ENROLLED
}


extend type Query {
    coursesPage(category: CourseCategory): CoursesPage!
}