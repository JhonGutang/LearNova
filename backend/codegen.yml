schema: "src/features/**/*.graphql"
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
      - "typescript-operations"
    config:
      useIndexSignature: true
      contextType: "../config/context#GraphQLContext"
