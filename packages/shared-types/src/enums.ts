// Common Enums used across both applications

export enum Status {
  PUBLISHED = "PUBLISHED",
  DRAFT = "DRAFT",
  UNLISTED = "UNLISTED",
}

export enum Role {
  STUDENT = "STUDENT",
  CREATOR = "CREATOR",
}

