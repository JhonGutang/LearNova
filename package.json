{
  "name": "learning-management-system",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "creator-with-backend": "concurrently \"pnpm --filter backend dev\" \"pnpm --filter lms-creator dev\"",
    "init-backend": "cd packages/backend && npx prisma generate && npm run codegen"
  }
}
