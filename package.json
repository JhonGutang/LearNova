{
  "scripts": {
    "install-all-dependencies": "npm install && npm install --prefix backend && npm install --prefix lms-creator && npm install --prefix lms-student",

    "server": "npm run dev --prefix backend",
    "creator": "npm run dev --prefix lms-creator",
    "student": "npm run dev --prefix lms-student",

    "start-student": "concurrently \"npm run server\" \"npm run student\"",
    "start-creator": "concurrently \"npm run server\" \"npm run creator\"",
    "start-all": "concurrently \"npm run server\" \"npm run creator\" \"npm run student\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
